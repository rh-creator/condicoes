let nome = 'Rhuan'
let nota1 = 10  
let nota2 = 50
let media = (nota1 + nota2) / 2 
        console.log('Aluno:', nome)
    if (media >= 6) {
        console.log('Você foi aprovado com a nota', media)
    } else {
        console.log('você foi reprovado com a nota', media)
        
    }